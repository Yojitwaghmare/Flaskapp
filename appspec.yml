version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/flaskapp

hooks:
  ApplicationStop:
    - location: scripts/stop_server.sh
      runas: ec2-user

  ApplicationStart:
    - location: scripts/start_server.sh
      runas: ec2-user
